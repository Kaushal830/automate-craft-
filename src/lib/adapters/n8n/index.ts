/**
 * n8n adapter barrel.
 *
 * Concrete implementation lands in Milestone 6. The stub class exists
 * now so the registry can import it without breaking.
 */

export { N8nAdapter } from "./adapter";
